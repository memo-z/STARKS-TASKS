#include <stdio.h>

int main(){
	
int **(*(*ptr)(int x));
void *(*(*N[3])())();
/*
1.ptr is a pointer to a function taking an integer argument and returning a pointer to a pointer to an integer.

2.N is an array of 3 pointers to functions taking no arguments and returning a pointer to void.


*/


	
	return 0;
}